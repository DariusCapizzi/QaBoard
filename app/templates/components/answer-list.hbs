
<!-- {{sorting-options sendDate="dateSort" sendKarma="karmaSort"}} -->
{{#each answers as |answer|}}

  {{#if answer.parentAnswer}}

  {{#if isReply}}
  {{answer-tile answer=answer sendAnswer="saveAnswer" question=question}}
  {{/if}}
  {{else}}
  {{answer-tile answer=answer sendAnswer="saveAnswer" question=question}}

  {{/if}}
{{/each}}
