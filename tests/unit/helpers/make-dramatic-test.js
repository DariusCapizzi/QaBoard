import { makeDramatic } from 'qa-board/helpers/make-dramatic';
import { module, test } from 'qunit';

module('Unit | Helper | make dramatic');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = makeDramatic([42]);
  assert.ok(result);
});
